{% block navigation %}

    <nav>
        <div class="link-one">
            <a href="{{ path('app_profil') }}" id="Profil">Profil</a>
            <a href="{{ path('app_abonnement') }}" id="Abonnement">Abonnement</a>
        </div>
        <div class="link-two">
            <a href="{{ path('app_pdf_generator') }}" id="Pdf">PDF Generator</a>
            {% if app.user %}
                <a href="{{ path('app_logout') }}" id="Logout">Logout</a>
            {% endif %}
        </div>
    </nav>

{% endblock %}